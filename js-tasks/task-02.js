// Make negative

function makeNegative(num) {
    return num > 0 ? -num : num;
}

//Лущиков 
function makeNegative(num) {
    
    if (num > 0) {
        return -num; 
    } else {
        return num; 
    }
}